# Voice (Dev D)

ElevenLabs integration and demo audio assets.

## Hello world

After activating ElevenLabs Creator tier:
1. Copy API key to `.env.local` as `ELEVENLABS_API_KEY`
2. Document default `voice_id` in root README Integration Contracts

## Scripts

Add generation scripts under `scripts/` — keep committed audio files small (demo samples only).
