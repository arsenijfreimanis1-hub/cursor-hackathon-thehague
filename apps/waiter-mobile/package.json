{
  "name": "@rekentafel/waiter-mobile",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "pnpm --filter @rekentafel/staff-web build",
    "cap:sync": "cap sync",
    "cap:open:ios": "cap open ios",
    "cap:open:android": "cap open android",
    "prepare:ios": "pnpm build:web && cap sync ios",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "@capacitor/ios": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0",
    "@rekentafel/config": "workspace:*",
    "@types/node": "^22.15.32",
    "typescript": "^5.8.3"
  }
}
