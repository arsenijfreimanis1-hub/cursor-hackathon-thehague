# Frontend (Dev A)

Next.js or Vite app — scaffold at kickoff once idea is chosen.

```bash
npm create vite@latest . -- --template react-ts
# or: npx create-next-app@latest .
```

Env: `NEXT_PUBLIC_API_URL=http://localhost:4000`
