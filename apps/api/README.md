# API (Dev C)

```bash
cp ../../.env.example ../../.env.local   # from repo root
npm install
npm run dev
```

`GET /health` → `{ "status": "ok" }`

Document new endpoints in root `README.md` Integration Contracts.
